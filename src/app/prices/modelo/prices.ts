export interface Prices {
    id: number,
    brandId: number,
    startDate: string,
    endDate: string,
    priceList: number,
    productId: number,
    priority: number,
    price: number,
    currency: string
}
